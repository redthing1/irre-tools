
# src level meson

# vars

include = include_directories('.')

subdir('irre') # IRRE library

subdir('asmr') # assembler
subdir('disasm') # disassembler
subdir('emu') # emulator
# subdir('legc') # converter
